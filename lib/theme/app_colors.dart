import 'package:flutter/material.dart';

/// Định nghĩa màu sắc từ Figma Design System
/// Ứng dụng: Quản lí lịch nhắc sức khỏe
class AppColors {
  // ── PRIMARY ──────────────────────────────────────
  static const Color primary          = Color(0xFF00ACB2);
  static const Color primaryDark      = Color(0xFF008A8F);
  static const Color primaryLight     = Color(0xFFE8F8F7);
  static const Color kPrimaryLight    = Color(0xFFE8F8F7);
  static const Color primaryTint10    = Color(0xFFE8F8F7);
  static const Color primaryTint30    = Color(0xFFB2E8EA);

  // ── GRADIENT ─────────────────────────────────────
  static const Color gradientStart    = Color(0xFFE8F8F7);
  static const Color gradientEnd      = Color(0xFFF0F8F7);

  // ── BACKGROUND ───────────────────────────────────
  static const Color background       = Color(0xFFF0F8F7);
  static const Color surface          = Color(0xFFFFFFFF);
  static const Color card             = Color(0xFFFFFFFF);
  static const Color cardBackground   = Color(0xFFFFFFFF);
  static const Color selectedCardBg   = Color(0xFFE8F8F7);
  static const Color inputBackground  = Color(0xFFF5F5F5);

  // ── TEXT ─────────────────────────────────────────
  static const Color textDark         = Color(0xFF0C1D1A);
  static const Color textPrimary      = Color(0xFF0C1D1A);
  static const Color textBody         = Color(0xFF2D2D2D);
  static const Color textSecondary    = Color(0xFF6B7280);
  static const Color textGrey         = Color(0xFF9E9E9E);
  static const Color textHint         = Color(0xFFB0BEC5);
  static const Color textMuted        = Color(0xFFB0BEC5);
  static const Color textSlate        = Color(0xFF64748B);
  static const Color textSlateLight   = Color(0xFF94A3B8);
  static const Color navInactive      = Color(0xFF9E9E9E);

  // ── TEAL VARIANTS ────────────────────────────────
  static const Color tealTint         = Color(0xFFB2E8EA);
  static const Color tealMuted        = Color(0xFF80CBC4);

  // ── BORDER ───────────────────────────────────────
  static const Color border           = Color(0xFFE2E8F0);
  static const Color borderLight      = Color(0xFFEEF2F6);
  static const Color borderPrimary    = Color(0xFF00ACB2);
  static const Color inputBorder      = Color(0xFFE2E8F0);
  static const Color handleBar        = Color(0xFFCFD8DC);

  // ── SHADOW ───────────────────────────────────────
  static const Color shadow           = Color(0xFF000000);
  static const Color shadowPrimary    = Color(0xFF00ACB2);

  // ── ICON BACKGROUNDS ─────────────────────────────
  static const Color iconBgTeal       = Color(0xFFE8F8F7);
  static const Color iconBgYellow     = Color(0xFFFFF8E1);
  static const Color iconBgPink       = Color(0xFFFCE4EC);

  // ── REMINDER COLORS ──────────────────────────────
  static const Color reminderPink     = Color(0xFFFCE4EC);
  static const Color reminderYellow   = Color(0xFFFFF8E1);
  static const Color reminderBlue     = Color(0xFFE3F2FD);

  // ── STATUS ───────────────────────────────────────
  static const Color info             = Color(0xFF2196F3);
  static const Color success          = Color(0xFF43A047);
  static const Color warning          = Color(0xFFFB8C00);
  static const Color error            = Color(0xFFE53935);

  // ── DIVIDER ──────────────────────────────────────
  static const Color divider          = Color(0xFFEEF2F6);

  // ── BASIC ────────────────────────────────────────
  static const Color white            = Color(0xFFFFFFFF);
  static const Color black            = Color(0xFF000000);
}
